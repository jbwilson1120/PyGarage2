<?php

/**
 * Util short summary.
 *
 * Util description.
 *
 * @version 1.0
 * @author jbwil
 */
class Util
{
    public function CheckPinState($Pin){
        // Returns the state of a given pin
        if (empty($Pin)) {
            return -1;
        }else{
            if()
        }
    }
}